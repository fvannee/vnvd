import System;

namespace Tests
{
   class public ContextIncorrect
   {
      public static void Main(String[] argv)
      {
         Int32 x, y;
         const Int32 z = 1;
         z = 10;
         12 = 10;
         x + y = 10;
         
         - 'a';
         + true;
         
         Char c;
         !c;
         Boolean b;
         b + 10 * c % 2;
         'a' < c;
         b && 10 || c;
         
         'a' = b;
         b != 10;
         x + y = c;
         
         c = x + y;
         b = 10;
         p; q;
         Boolean q;
         
         { 2 + 4 * 3; Int32 w; };
         
         read(x, y) + 10;
         
         'c' != write(x, y);
         10 + { Int32 u, v; read(u, v); write(u, v); };
      }
   }
}